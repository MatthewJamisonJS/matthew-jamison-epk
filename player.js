// player.js — the one player for / and every /music/<slug>/.
//
// PLACEHOLDER. The markup contract landed first (scripts/build.mjs owns the bar
// and the per-release #store-data block); the machine — transport, scrubber,
// quality modes, the offline vault — arrives with the next task, lifted out of
// script.js and release.js. Until then this file exists so the pages that
// reference it get a 200, not a 404.
(function () {})();
